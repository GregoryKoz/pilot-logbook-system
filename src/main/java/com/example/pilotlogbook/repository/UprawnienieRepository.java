package com.example.pilotlogbook.repository;

import com.example.pilotlogbook.entity.Uprawnienie;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UprawnienieRepository extends JpaRepository<Uprawnienie, Long> {
}